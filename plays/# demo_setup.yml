# demo_setup.yml

- hosts: localhost
  connection: local
  gather_facts: False
  tasks:
    - name: Provision a set of instances
      ec2:
         key_name: ~/.ssh/ronak-nathani.pem
         group: test
         instance_type: t2.micro
         image: ""
         wait: true
         exact_count: 5
         count_tag:
            Name: Demo
         instance_tags:
            Name: Demo
      register: ec2